{
  "name": "ghost-preset-toolbar",
  "version": "0.1.0",
  "private": true,
  "description": "MV3 Chromium extension: preset toolbar for Ghost Admin (Phase-1 scaffold).",
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "manifest:validate": "node scripts/validate-manifest.mjs",
    "build": "tsc --noEmit && node scripts/build.mjs",
    "verify": "npm run format:check && npm run lint && npm run typecheck && npm run build && npm test && npm run manifest:validate"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.1",
    "@types/node": "^26.2.0",
    "@typescript-eslint/eslint-plugin": "^8.44.0",
    "@typescript-eslint/parser": "^8.44.0",
    "eslint": "^9.35.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.44.0",
    "vitest": "^3.2.4"
  }
}
